version: '3.8'

services:
  ml-app:
    build: .
    container_name: iris-ml-app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./static:/app/static
    restart: unless-stopped
